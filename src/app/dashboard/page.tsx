import { PhasePlaceholder } from "@/components/phase-placeholder"

export const metadata = { title: "Dashboard · Cold Call Coach" }

export default function DashboardPage() {
  return (
    <PhasePlaceholder
      title="Dashboard"
      phase="Phase 7"
      summary="Three questions and nothing else: am I doing the work, am I getting better, who do I call next. Not built yet — Phase 0 shipped the shell and the design system only."
    />
  )
}
