import { PhasePlaceholder } from "@/components/phase-placeholder"

export const metadata = { title: "Leads · Cold Call Coach" }

export default function LeadsPage() {
  return (
    <PhasePlaceholder
      title="Leads"
      phase="Phase 4"
      summary="Five live kanban columns plus a collapsed terminal rail, drag-drop with edge autoscroll, and the lead add/edit modal. Not built yet — Phase 0 shipped the shell and the design system only."
    />
  )
}
